name: SFC Pass 27 Encounter Warning

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/pass27-encounter.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply encounter presentation pass
        run: python scripts/pass27_encounter.py
      - name: Install dependencies
        run: npm ci
      - name: Puzzle RPG TypeScript check
        run: npx tsc -p tsconfig.puzzle.json --noEmit
      - name: Production build
        run: bash scripts/sites-env.sh -- ./node_modules/.bin/vinext build
      - name: Commit pass
        shell: bash
        run: |
          rm .github/workflows/pass27-encounter.yml scripts/pass27_encounter.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/rpg/RPGMode.tsx app/rpg/RPGMode.module.css PROGRESS.md .github/workflows/pass27-encounter.yml scripts/pass27_encounter.py
          git commit -m "Implement SFC visual reconstruction pass 27"
          git push origin HEAD:main
