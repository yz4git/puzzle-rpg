name: Pass 35 Release Candidate Stability

on:
  push:
    paths:
      - '.github/workflows/pass35-release-stability.yml'

permissions:
  contents: write

jobs:
  stability:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Targeted PWA startup tests
        run: npm run test:pwa
      - name: Install browser stability runtime
        run: |
          npm install --no-save --package-lock=false --legacy-peer-deps playwright@1.55.0
          npx playwright install --with-deps chromium
      - name: Puzzle RPG TypeScript check
        run: npx tsc -p tsconfig.puzzle.json --noEmit
      - name: Production build
        run: bash scripts/sites-env.sh -- ./node_modules/.bin/vinext build
      - name: Start local game
        run: |
          npm run dev -- --host 127.0.0.1 --port 4173 >/tmp/pass35-vite.log 2>&1 &
          for i in {1..45}; do
            if curl -fsS http://127.0.0.1:4173 >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/pass35-vite.log
          exit 1
      - name: Safari-style release stability browser audit
        run: node scripts/pass35_stability_qa.mjs
      - name: Commit stability record
        run: |
          printf '%s\n' '' '## Release Candidate Stability Pass 35' '- Ran targeted PWA/startup tests plus browser-driven iPhone Safari-style stability QA after a clean TypeScript check and production build.' '- Verified build-id Service Worker registration, controlled reload, offline app-shell reload, RPG opening autosave, immediate reload/CONTINUE persistence, pagehide persistence and rapid mode transitions.' '- No gameplay balance, encounter tables, rewards, maps or battle math were changed by this stability audit.' >> PROGRESS.md
          rm -f scripts/pass35_stability_qa.mjs .github/workflows/pass35-release-stability.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Complete release candidate stability pass 35'
          git push
