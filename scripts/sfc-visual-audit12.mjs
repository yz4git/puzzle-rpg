import { mkdir, writeFile } from 'node:fs/promises';
import { spawn } from 'node:child_process';

const out='sfc-visual-audit12'; await mkdir(out,{recursive:true});
const sleep=(ms)=>new Promise(r=>setTimeout(r,ms));
const browser=spawn('google-chrome',['--headless=new','--no-sandbox','--disable-gpu','--hide-scrollbars','--remote-debugging-port=9222','--user-data-dir=/tmp/sfc12','--window-size=402,690','about:blank'],{stdio:['ignore','ignore','inherit']});
async function waitJson(url){for(let i=0;i<240;i++){try{const r=await fetch(url);if(r.ok)return r.json();}catch{}await sleep(250);}throw new Error('CDP timeout');}
class CDP{constructor(url){this.ws=new WebSocket(url);this.id=0;this.pending=new Map();this.ready=new Promise((resolve,reject)=>{this.ws.addEventListener('open',resolve,{once:true});this.ws.addEventListener('error',reject,{once:true});});this.ws.addEventListener('message',e=>{const m=JSON.parse(e.data),p=this.pending.get(m.id);if(!p)return;this.pending.delete(m.id);m.error?p.reject(new Error(m.error.message)):p.resolve(m.result);});}async send(method,params={}){await this.ready;const id=++this.id;return new Promise((resolve,reject)=>{this.pending.set(id,{resolve,reject});this.ws.send(JSON.stringify({id,method,params}));});}}
async function ev(cdp,expression){const r=await cdp.send('Runtime.evaluate',{expression,awaitPromise:true,returnByValue:true});if(r.exceptionDetails)throw new Error(r.exceptionDetails.text);return r.result.value;}
async function waitFor(cdp,expression,timeout=12000){const end=Date.now()+timeout;while(Date.now()<end){if(await ev(cdp,expression))return true;await sleep(100);}return false;}
async function click(cdp,text){return ev(cdp,`(()=>{const e=[...document.querySelectorAll('button')].find(x=>(x.innerText||'').includes(${JSON.stringify(text)}));if(!e)return false;e.click();return true})()`);}
function saveExpr(mapId,x,y,direction='up'){const s={version:1,playerName:'LIO',level:1,exp:0,hp:20,maxHp:20,gold:18,mapId,position:{x,y},direction,lastInn:{mapId:'hearthVillage',position:{x:8,y:10}},inventory:[{id:'herb',count:2},{id:'smoke',count:1}],inventorySlots:4,equipmentOwned:['travellerCoat'],equipment:{weapon:null,armor:null,charm:null},techniques:[],techniqueSlots:2,memos:[],flags:['story:openingSeen'],openedChests:[],defeatedEncounters:[],defeatedEnemies:{},releasedEnemies:{},battleLog:[],steps:8,playSeconds:60,encounterMeter:99,settings:{music:false,sfx:false}};return `localStorage.setItem('puzzle-rpg:rpg-mode:v1',${JSON.stringify(JSON.stringify(s))});true`;}
async function enter(cdp){await waitFor(cdp,`document.body.innerText.includes('RPG MODE')`,15000);await click(cdp,'RPG MODE');await sleep(120);await click(cdp,'CONTINUE');if(!(await waitFor(cdp,`!!document.querySelector('canvas[aria-label$="exploration map"]')`,8000)))throw new Error('map missing');await sleep(260);}
async function teleport(cdp,mapId,x,y){await ev(cdp,saveExpr(mapId,x,y));await cdp.send('Page.reload',{ignoreCache:true});await sleep(420);await enter(cdp);}
async function shot(cdp,name,note){const metrics=await ev(cdp,`(()=>{const controls=document.querySelector('[aria-label="RPG touch controls"]');const d=document.querySelector('[aria-label="RPG touch controls"] button[aria-label="Move up"]');const a=[...document.querySelectorAll('button')].find(x=>(x.innerText||'').includes('CHECK'));const cr=controls?.getBoundingClientRect(),dr=d?.getBoundingClientRect(),ar=a?.getBoundingClientRect();return{scroll:document.documentElement.scrollHeight,controls:cr?{h:Math.round(cr.height)}:null,dpad:dr?{w:Math.round(dr.width),h:Math.round(dr.height)}:null,a:ar?{w:Math.round(ar.width),h:Math.round(ar.height)}:null,text:document.body.innerText.replace(/\\s+/g,' ').slice(0,260)}})()`);const {data}=await cdp.send('Page.captureScreenshot',{format:'png',fromSurface:true});await writeFile(`${out}/${name}.png`,Buffer.from(data,'base64'));return{name,note,metrics};}
const records=[];
try{const pages=await waitJson('http://127.0.0.1:9222/json');const cdp=new CDP((pages.find(p=>p.type==='page')||pages[0]).webSocketDebuggerUrl);await cdp.send('Page.enable');await cdp.send('Runtime.enable');await cdp.send('Emulation.setDeviceMetricsOverride',{width:402,height:690,deviceScaleFactor:3,mobile:true,screenWidth:402,screenHeight:874});await cdp.send('Page.navigate',{url:'http://127.0.0.1:5173'});await waitFor(cdp,`document.body.innerText.includes('RPG MODE')`,15000);
await teleport(cdp,'hearthVillage',8,10);records.push(await shot(cdp,'01-hearth-control-deck','Warm town palette control deck'));
await teleport(cdp,'emberShrine',8,8);records.push(await shot(cdp,'02-ember-control-deck','Red shrine palette control deck'));
for(const r of records){if(r.metrics.scroll!==690)throw new Error(`${r.name}: overflow ${r.metrics.scroll}`);if(!r.metrics.dpad||r.metrics.dpad.w<34)throw new Error(`${r.name}: dpad hit area regression`);if(!r.metrics.a||r.metrics.a.w<58)throw new Error(`${r.name}: A hit area regression`);}await writeFile(`${out}/audit.json`,JSON.stringify(records,null,2));cdp.ws.close();}finally{browser.kill('SIGTERM');}
